# Copy to .env and fill in for production.
# If MONGODB_URI is empty, the server boots an in-memory MongoDB (dev only).

# Production: MongoDB Atlas connection string
MONGODB_URI=

# Port the API listens on
PORT=4000

# Shared secret the MT5 producer sends in the x-ingest-token header
INGEST_TOKEN=dev-token
